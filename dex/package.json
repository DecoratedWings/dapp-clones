{
  "name": "dex",
  "version": "1.0.0",
  "description": "accessible dapp clone of a dex",
  "main": "app.js",
  "scripts": {
    "start": "browser-sync start --server --files \"*.html, css/*.css, js/*.js\""
  },
  "devDependencies": {
    "browser-sync": "^2.29.3"
  }
}
